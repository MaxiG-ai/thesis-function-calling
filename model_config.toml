# --- Model Registry ---
# Static definitions of available models and their capabilities.

[models.gpt-5-mini]
litellm_name = "openai/gpt-5-mini"
context_window = 128000
provider = "aicore"
api_base = "http://localhost:3030/v1"     # 👈 Point to your proxy (ensure /v1 is correct for your setup)
api_key = "THINKTANK"                  # Proxies usually require a non-empty string

[models.gpt-4-1]
litellm_name = "openai/gpt-4.1"
context_window = 128000
provider = "aicore"
api_base = "http://localhost:3030/v1"     # 👈 Point to your proxy (ensure /v1 is correct for your setup)
api_key = "THINKTANK"                  # Proxies usually require a non-empty string